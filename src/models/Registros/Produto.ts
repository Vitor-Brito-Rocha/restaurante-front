
export interface Produto {
    id?: number,
    descricao?: string,
    nome?: string,
    imagem_url?: string,
    preco?: number,
    categoria_id?: number,
    disponivel?: boolean,
    visibilidade_menu?: boolean,
}